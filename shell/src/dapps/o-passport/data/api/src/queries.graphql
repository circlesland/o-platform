query sessionInfo {
    sessionInfo {
        isLoggedOn
        hasProfile
        profileId
    }
}

query myProfile {
    profiles(query:{}) {
        id
        circlesAddress
        circlesSafeOwner
        firstName
        lastName
        dream
        country
        avatarUrl
        avatarCid
        avatarMimeType
    }
}

query profiles($id:Int!) {
    profiles(query:{id: $id}) {
        id
        circlesAddress
        circlesSafeOwner
        firstName
        lastName
        dream
        country
        avatarUrl
        avatarCid
        avatarMimeType
    }
}