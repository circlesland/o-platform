mutation requestIndexTransaction($data:RequestIndexTransactionInput!) {
    requestIndexTransaction(data: $data) {
        id
        transactionHash
        tags {
            id
            typeId
            value
        }
    }
}